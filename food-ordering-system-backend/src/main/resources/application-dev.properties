# Dev profile: H2 in-memory database so the app runs without MySQL installed.
spring.datasource.url=jdbc:h2:mem:food_ordering_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Run data.sql after Hibernate creates the schema
spring.jpa.defer-datasource-initialization=true

# H2 web console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
